import { FC } from "react";

type LogoProps = {
  label: string;
};

const Logo: FC<LogoProps> = ({}) => {
  return (
    <div>
      {/* <link className="w-[300px] h-[300px]" href="/logo.svg"></link> */}
    </div>
  );
};
export default Logo;
