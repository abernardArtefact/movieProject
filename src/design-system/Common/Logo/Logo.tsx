import { FC } from "react";

type LogoProps = {
  label: string;
  ternaryLogo: boolean;
  types: Array<{ id: number; name: string; url: string }>;
};

const Logo: FC<LogoProps> = ({}) => {
  return <div></div>;
};
export default Logo;
